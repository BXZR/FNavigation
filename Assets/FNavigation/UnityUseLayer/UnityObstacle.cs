using org.critterai.nav;
using org.critterai.nav.u3d;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UnityObstacle : MonoBehaviour
{

    void Start()
    {
        
    }

    public ScriptableObject mesh;

    public void ReBake()
    {
        /*
        TileBuildData BuildData 


        INavmeshData theNavMesh = (INavmeshData)mesh;
        //NavmeshTile theTile  = theNavMesh.GetNavmesh().GetTile(1,1,1);
        NavmeshBuildInfo configUse = new NavmeshBuildInfo();

        theNavMesh.Load(,theNavMesh.BuildInfo);
        NavmeshTileData[] tiles = null;
         */
    }
   
}
